import 'package:flutter/material.dart';

final BorderRadiusGeometry borderRadiusIphone = BorderRadius.circular(45);
const BorderRadiusGeometry borderRadiusList = BorderRadius.vertical(
  top: Radius.circular(20),
  bottom: Radius.circular(45),
);

final BorderRadiusGeometry borderRadiusCuadroTextoNoticia =
    BorderRadius.circular(30);
